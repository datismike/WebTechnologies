package controller.command.impl;

import controller.command.ICommand;

public class RemoveBaseObject implements ICommand {
    @Override
    public String execute(String request) {
        return null;
    }
}